spring.application.name=backend

# Comment out MySQL configuration for now
# spring.datasource.url = jdbc:mysql://localhost:3306/product-catalog?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username = root
# spring.datasource.password = {enter your password here}
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Use H2 in-memory database for testing
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.jpa.hibernate.ddl-auto=update    
spring.jpa.show-sql=true
spring.jpa.hibernate.format_sql=true

# Disable Flyway for H2 testing
spring.flyway.enabled=false
# spring.flyway.baseline-on-migrate=true

# Date format configuration
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=UTC